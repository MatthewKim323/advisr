"use client";

/**
 * The knowledge-graph constellation that grows next to Archivist's desk as
 * claims enter the graph. Click a node → modal with claim + evidence +
 * confidence. Load-bearing for the ingestion shot in the demo video.
 */
export default function Constellation() {
  // TODO: force-directed layout over confirmed claims; emit hover state
  // back into the canvas so the active claim's source file highlights.
  return null;
}
