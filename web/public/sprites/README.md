# Sprites

Donarg's Office Interior ($2) — per PLAN.md §11, decision locked. Drop the
pack here after purchase:

```
public/sprites/
├── office/               # floor, walls, desks, chairs, computers
├── characters/           # 7 agent sprites, Student sprite
└── overlays/             # laptop screens, speech bubbles, particles
```

Supplement with Kenney.nl (CC0) where the pack is thin. NEVER hand-draw
sprites — one-line rule from PLAN.md §10.
