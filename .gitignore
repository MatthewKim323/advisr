# Deps
node_modules/
.pnp
.pnp.js

# Builds
dist/
build/
.next/
out/

# Env
.env
.env.local
.env.*.local
!.env.example

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Editor
.vscode/
.idea/
.DS_Store
*.swp
*.swo

# TypeScript
*.tsbuildinfo

# Browser-use cache (large binary blobs — regenerate from scripts)
web/lib/browser/.cache/

# Private strategy doc — local only
PLAN.md

# Paid sprite pack — Donarg's license prohibits redistribution.
# Purchase separately at itch.io and drop back into the repo root.
Office Tileset/

# Kenney RPG Urban Pack — CC0, but we don't want asset blobs in the repo.
# Download at kenney.nl/assets/rpg-urban-pack and drop back into the repo root.
kenney_rpg-urban-pack/

# Copied sprite assets served by Next.js. Populated locally from the packs above.
# Not committed for the same licensing + repo-bloat reasons.
web/public/sprites/office/
web/public/sprites/chars/
