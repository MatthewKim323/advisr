# Maria Isabel Delgado Santos

**Her artifacts live in `/about/` at repo root** — moving them requires
updating every worker-loading path, so for now this folder is just a pointer.

See:
- `/about/00_maria_summary.md` — biographical reference
- `/about/01_transcript.txt` through `/about/08_expected_claims.md` — sources + rubric

At seed time (`npm run seed` in `web/`), Maria's files are registered as
`source_files` rows and her expected claims become the demo-mode ground truth
that workers return when DEMO_MODE=true.

### The callback choreography

1. Common App + UC PIQ both lean on grandmother → Draft detects cross-essay redundancy
2. Voice memo contains Mr. Arellano propeller story → Draft surfaces as stronger angle
3. Confirmation photo + St. Anthony's → Catholic → Match-Maker adds Santa Clara / LMU / USD
4. $47,200 AGI + Pell eligibility → Bursar proves meet-full-need privates < CSUs
5. Voice memo "dream_school: Harvey Mudd" → Match-Maker validates reach she won't admit
